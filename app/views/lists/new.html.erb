<h1>Add a list</h1>

<%= simple_form_for [@list] do |f| %>
	<%= f.input :name %>
	<%= f.submit "Create List", class: "btn btn-primary m-2" %>
<% end %>

<%= link_to "back", lists_path, class:"btn btn-secondary m-2" %>
